const BASE_URL = 'http://localhost:3001/';
const LOGIN = `${BASE_URL}login`;
const GAMES_LIST = `${BASE_URL}games`;
const CATEGORIES_LIST = `${BASE_URL}categories`;
const LOGOUT = `${BASE_URL}logout`;

export default {
	LOGIN,
	GAMES_LIST,
	CATEGORIES_LIST,
	LOGOUT
};